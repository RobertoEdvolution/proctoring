/**
 * Quiz finish module — intercepts the "Submit all and finish" form on the
 * summary page and triggers the Cloud Run analysis web service before the
 * form is actually submitted.  Works transparently with Moodle's own
 * confirmation modal: we hook the form's submit() method so *any* code
 * path that submits the form (button click, confirmation dialog, timer)
 * goes through our interceptor first.
 *
 * @module     quizaccess_proctoring/quizfinish
 * @copyright  2024 Brain Station 23
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define(["core/ajax"],function(t){var e=!1,i=function(i,n){if(!e){e=!0;var u=function(){HTMLFormElement.prototype.submit.call(i)},a=setTimeout(u,1e4);t.call([{methodname:"quizaccess_proctoring_trigger_analysis",args:{courseid:n.courseid,quizid:n.quizid,attemptid:n.status}}])[0].always(function(){clearTimeout(a),u()})}};return{init:function(t){var n=document.getElementById("frm-finishattempt");n&&(n.submit=function(){i(n,t)},n.addEventListener("submit",function(u){e||(u.preventDefault(),i(n,t))}))}}});